---
name: Bug report
about: Something isn't working the way you expected
title: ''
labels: bug
assignees: ''
---

**What happened**
A clear description of the bug.

**What you expected**
What you thought would happen instead.

**How to reproduce**
The exact command or MSBuild configuration you used, e.g.:

```
nuget-cooldown check MyApp.sln --days 14
```

If you can, attach a minimal `project.assets.json` or a small sample project that shows the problem.

**Output**
Paste the relevant output (run with `--verbose`, or `--format json`, if helpful).

**Environment**
- NuGetCooldown version:
- How you're using it (dotnet tool / NuGetCooldown.MSBuild):
- OS:
- `dotnet --version`:
